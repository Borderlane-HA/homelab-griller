services:
  homelab-griller:
    build: .
    container_name: homelab-griller
    restart: unless-stopped
    ports:
      - "8088:80"
    environment:
      TZ: Europe/Berlin
      GRILLER_ADMIN_PASSWORD: ${GRILLER_ADMIN_PASSWORD:-change-me-now}
    volumes:
      - ./data:/var/www/html/data
